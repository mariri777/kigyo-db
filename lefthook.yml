pre-commit:
  parallel: true
  jobs:
    - name: eslint
      glob: "*.{js,jsx,ts,tsx,mjs,cjs,mts}"
      run: pnpm exec eslint {staged_files}
    - name: typecheck
      glob: "*.{ts,tsx,mts}"
      run: pnpm typecheck
